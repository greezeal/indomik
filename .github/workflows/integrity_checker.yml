name: Chapter Integrity Checker

on:
  schedule:
    - cron: "0 */3 * * *" # Menjalankan setiap 3 jam sekali
  workflow_dispatch: # Memungkinkan menjalankan manual dari tab Actions

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scraper/requirements.txt

      - name: Run Integrity Checker (All Existing Comics)
        run: |
          python -u scraper/integrity_checker.py --all --delay 2

      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff-index --quiet HEAD || git commit -m "chore: integrity check results [$(date +'%Y-%m-%d %H:%M')]"
          git push
